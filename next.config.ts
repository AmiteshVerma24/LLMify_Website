import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["unsplash.com", "assets.aceternity.com", "lh3.googleusercontent.com"],
  }
  /* config options here */
};

export default nextConfig;
